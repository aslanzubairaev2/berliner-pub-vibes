# Инструкция по развертыванию сайта Berliner Pub на хостинге STRATO

Эта инструкция описывает полный процесс развертывания веб-сайта Berliner Pub на немецком хостинге STRATO, начиная с настройки базы данных Supabase и заканчивая запуском сайта в production.

---

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Часть 1: Настройка Supabase (База данных)](#часть-1-настройка-supabase-база-данных)
3. [Часть 2: Сборка проекта](#часть-2-сборка-проекта)
4. [Часть 3: Настройка хостинга STRATO](#часть-3-настройка-хостинга-strato)
5. [Часть 4: Загрузка файлов на STRATO](#часть-4-загрузка-файлов-на-strato)
6. [Часть 5: Настройка безопасности](#часть-5-настройка-безопасности)
7. [Часть 6: Заполнение базы данных](#часть-6-заполнение-базы-данных)
8. [Часть 7: Проверка и тестирование](#часть-7-проверка-и-тестирование)
9. [Часть 8: Обслуживание и обновления](#часть-8-обслуживание-и-обновления)
10. [Решение проблем](#решение-проблем)

---

## Предварительные требования

### Необходимое программное обеспечение:

1. **Node.js** (версия 18 или выше) - [скачать](https://nodejs.org/)
2. **Git** - [скачать](https://git-scm.com/)
3. **FTP/SFTP клиент** - например, [FileZilla](https://filezilla-project.org/) (бесплатно)
4. **Текстовый редактор** - VSCode, Sublime Text или любой другой

### Необходимые аккаунты:

1. **Supabase аккаунт** - [создать бесплатно](https://supabase.com/)
2. **STRATO хостинг** - ваш существующий аккаунт
3. **Домен** - ваш домен, зарегистрированный в STRATO

### Информация, которая вам понадобится:

- FTP/SFTP данные доступа к STRATO (хост, логин, пароль)
- Название вашего домена
- Email администратора для Supabase

---

## Часть 1: Настройка Supabase (База данных)

### Шаг 1.1: Создание проекта Supabase

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Нажмите **"Sign In"** и войдите через GitHub или Email
3. Нажмите **"New Project"**
4. Заполните данные:
   - **Name**: `berliner-pub-production` (или любое другое имя)
   - **Database Password**: **СОХРАНИТЕ ЭТОТ ПАРОЛЬ!** Он понадобится позже
   - **Region**: Выберите `Frankfurt (eu-central-1)` (ближайший к Германии)
   - **Pricing Plan**: Можете начать с Free tier
5. Нажмите **"Create new project"**
6. Подождите 1-2 минуты, пока проект создается

### Шаг 1.2: Получение учетных данных API

После создания проекта:

1. В боковом меню нажмите на значок **"Settings"** (шестеренка)
2. Выберите **"API"**
3. **СОХРАНИТЕ** следующие данные (они понадобятся позже):
   - **Project URL**: `https://xxxxxxxxxxxxx.supabase.co`
   - **anon/public key**: длинный JWT токен начинающийся с `eyJ...`
   - **Project ID**: короткий буквенно-цифровой идентификатор

### Шаг 1.3: Выполнение SQL миграций

Теперь нужно создать структуру базы данных:

1. В боковом меню Supabase нажмите **"SQL Editor"**
2. Нажмите **"New query"**
3. Откройте папку проекта и найдите файлы миграций в `supabase/migrations/`
4. **ВАЖНО**: Выполняйте миграции **ПО ПОРЯДКУ** (по дате в имени файла)

**Порядок выполнения:**

```bash
# Список всех файлов миграций (выполнять в этом порядке):
1. 20251105215717_7f3cca0a-23c7-4760-967a-c23214217ad4.sql
2. 20251115154304_e622d2e4-4587-46bc-9999-78a69805fe8e.sql
```

Для каждого файла:
1. Откройте файл в текстовом редакторе
2. Скопируйте **ВСЕ** содержимое
3. Вставьте в SQL Editor в Supabase
4. Нажмите **"Run"** (или F5)
5. Убедитесь, что нет ошибок (должно показать "Success")
6. Переходите к следующему файлу

### Шаг 1.4: Создание Storage Bucket для изображений

1. В боковом меню Supabase нажмите **"Storage"**
2. Нажмите **"Create a new bucket"**
3. Заполните:
   - **Name**: `drink-images`
   - **Public bucket**: ✅ **ДА** (отметьте галочку)
4. Нажмите **"Create bucket"**

### Шаг 1.5: Настройка RLS политик для Storage

После создания bucket:

1. Нажмите на bucket `drink-images`
2. Перейдите на вкладку **"Policies"**
3. Нажмите **"New Policy"**
4. Выберите **"For full customization, create a policy"**
5. Создайте следующие политики:

**Политика 1: Публичное чтение**
```sql
-- Policy name: Public read access
CREATE POLICY "Public read access"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'drink-images');
```

**Политика 2: Загрузка для аутентифицированных**
```sql
-- Policy name: Authenticated users can upload
CREATE POLICY "Authenticated users can upload"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'drink-images');
```

**Политика 3: Удаление для аутентифицированных**
```sql
-- Policy name: Authenticated users can delete
CREATE POLICY "Authenticated users can delete"
ON storage.objects FOR DELETE
TO authenticated
USING (bucket_id = 'drink-images');
```

### Шаг 1.6: Настройка секретов для Edge Functions

1. В Supabase перейдите в **Settings** → **Edge Functions**
2. Прокрутите до раздела **"Function Secrets"**
3. Добавьте следующие секреты (если планируете использовать генерацию изображений):

```
HUGGING_FACE_ACCESS_TOKEN=ваш_токен_от_huggingface
```

> **Примечание**: Токен Hugging Face нужен только для функции генерации изображений пива. Если не планируете использовать, можете пропустить.

### Шаг 1.7: Создание первого администратора

После выполнения миграций нужно создать учетную запись администратора:

1. В **SQL Editor** выполните следующий запрос:

```sql
-- ЗАМЕНИТЕ email и password на свои данные!
INSERT INTO public.admin_users (name, email, password_hash, is_active)
VALUES (
  'Главный администратор',
  'admin@vash-domen.de',
  public.simple_hash('ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ_ЗДЕСЬ')
);
```

**ВАЖНО**: 
- Используйте **сильный пароль** (минимум 12 символов, буквы, цифры, символы)
- **СОХРАНИТЕ** email и пароль - они нужны для входа в админ-панель
- После первого входа **ОБЯЗАТЕЛЬНО** смените пароль через админ-панель

---

## Часть 2: Сборка проекта

### Шаг 2.1: Клонирование или получение проекта

Если проект в Git:
```bash
git clone https://github.com/ваш-репозиторий/berliner-pub.git
cd berliner-pub
```

Если проект в виде архива:
```bash
# Распакуйте архив в папку
cd путь/к/проекту
```

### Шаг 2.2: Установка зависимостей

```bash
npm install
```

Это установит все необходимые библиотеки (~260 пакетов). Процесс займет 2-5 минут.

### Шаг 2.3: Настройка переменных окружения

1. В корне проекта создайте файл `.env`:

```bash
# НЕ используйте VITE_ префикс - он не поддерживается!
# Вместо этого значения вшиты в код
```

2. Откройте файл `src/integrations/supabase/client.ts`
3. Замените значения на ваши данные из Supabase:

```typescript
const SUPABASE_URL = "https://ваш-project-id.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "ваш-anon-key-здесь";
```

### Шаг 2.4: Сборка production версии

```bash
npm run build
```

Эта команда:
- Компилирует TypeScript в JavaScript
- Минифицирует код
- Оптимизирует изображения
- Создает папку `dist/` с готовым сайтом

**Результат**: папка `dist/` содержит все файлы для загрузки на сервер.

### Шаг 2.5: Проверка локально (опционально)

Перед загрузкой на сервер можете протестировать:

```bash
npm run preview
```

Откройте `http://localhost:4173` в браузере и проверьте работу сайта.

---

## Часть 3: Настройка хостинга STRATO

### Шаг 3.1: Вход в STRATO Customer Center

1. Перейдите на [https://www.strato.de](https://www.strato.de)
2. Нажмите **"Login"** в правом верхнем углу
3. Выберите **"Kunden-Login"**
4. Введите ваши данные доступа
5. Перейдите в **"Produkte"** → выберите ваш хостинг пакет

### Шаг 3.2: Настройка FTP/SFTP доступа

1. В панели управления найдите раздел **"FTP-Zugänge"** или **"Zugang & Sicherheit"**
2. Запишите или создайте FTP данные:
   - **Hostname**: обычно `ftp.strato.de` или `ssh.strato.de`
   - **Username**: ваше имя пользователя
   - **Password**: ваш FTP пароль
   - **Port**: 21 (FTP) или 22 (SFTP)

**Рекомендация**: Используйте SFTP (порт 22) вместо FTP для безопасности.

### Шаг 3.3: Определение корневой папки сайта

В STRATO обычно структура такая:
```
/
├── logs/          # Логи сервера
├── ssl/           # SSL сертификаты
└── www/           # КОРНЕВАЯ ПАПКА САЙТА
    └── index.html # Сюда загружать файлы
```

**ВАЖНО**: Все файлы из папки `dist/` нужно загрузить в папку `www/`

---

## Часть 4: Загрузка файлов на STRATO

### Шаг 4.1: Подключение через FileZilla

1. Откройте FileZilla
2. Заполните в верхней панели:
   - **Host**: `sftp://ssh.strato.de` (или ваш SFTP хост)
   - **Username**: ваш логин
   - **Password**: ваш пароль
   - **Port**: `22`
3. Нажмите **"Quickconnect"** или **"Соединиться"**

### Шаг 4.2: Создание .htaccess файла

**КРИТИЧЕСКИ ВАЖНО** для работы React Router!

1. Создайте файл `.htaccess` в корне проекта (рядом с `dist/`)
2. Добавьте следующий код:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Если запрашиваемый файл существует, отдаем его
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  
  # Иначе перенаправляем на index.html
  RewriteRule ^ index.html [QSA,L]
</IfModule>

# Включаем сжатие
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Кэширование для статических файлов
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Безопасность
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set X-XSS-Protection "1; mode=block"
</IfModule>

# Блокируем доступ к .git и другим служебным файлам
<FilesMatch "^\.">
  Order allow,deny
  Deny from all
</FilesMatch>
```

### Шаг 4.3: Загрузка файлов

1. В FileZilla:
   - **Левая панель**: Найдите папку `dist/` вашего проекта
   - **Правая панель**: Перейдите в папку `www/` на сервере

2. Загрузите файлы:
   - Выделите **ВСЕ** содержимое папки `dist/`
   - Перетащите на правую панель (в `www/`)
   - Также загрузите файл `.htaccess`

3. Подождите завершения загрузки (обычно 2-5 минут)

**Структура на сервере должна быть:**
```
www/
├── .htaccess
├── index.html
├── assets/
│   ├── index-abc123.js
│   ├── index-def456.css
│   └── ...
├── favicon.ico
└── robots.txt
```

### Шаг 4.4: Проверка прав доступа

1. В FileZilla кликните правой кнопкой на папку `www/`
2. Выберите **"File permissions"**
3. Установите:
   - Папки: `755` (rwxr-xr-x)
   - Файлы: `644` (rw-r--r--)
4. Отметьте **"Recurse into subdirectories"**
5. Нажмите **"OK"**

---

## Часть 5: Настройка безопасности

### Шаг 5.1: Смена пароля администратора

**ОБЯЗАТЕЛЬНО** сделайте это после первого развертывания!

1. Откройте ваш сайт: `https://ваш-домен.de`
2. Перейдите на `/admin` или `/admin/login`
3. Войдите с данными, которые создали в Шаге 1.7
4. Перейдите в **Настройки** → **Безопасность**
5. Нажмите **"Сменить пароль"**
6. Введите:
   - Текущий пароль
   - Новый пароль (минимум 12 символов)
   - Подтверждение пароля
7. Сохраните изменения

### Шаг 5.2: Настройка SSL сертификата в STRATO

1. В STRATO Customer Center найдите **"SSL-Zertifikate"**
2. Для вашего домена активируйте **Let's Encrypt** сертификат (обычно бесплатно)
3. Подождите 5-10 минут для активации
4. Проверьте, что сайт открывается по `https://`

### Шаг 5.3: Настройка редиректа на HTTPS

Добавьте в начало файла `.htaccess`:

```apache
# Принудительный HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

### Шаг 5.4: Резервная копия конфигурации Supabase

1. Экспортируйте SQL схему:
   - В Supabase SQL Editor выполните:
   ```sql
   -- Эта команда покажет структуру всех таблиц
   SELECT table_name 
   FROM information_schema.tables 
   WHERE table_schema = 'public';
   ```

2. Сохраните результаты миграций в надежное место
3. Храните отдельно:
   - Supabase Project URL
   - Supabase Anon Key
   - Database Password

---

## Часть 6: Заполнение базы данных

### ВАЖНО: База данных пустая!

После миграций у вас есть **только структура** таблиц, но **нет данных**. Нужно заполнить:

### Шаг 6.1: Добавление напитков

**Вариант A: Через админ-панель (рекомендуется)**

1. Войдите в админ-панель: `https://ваш-домен.de/admin`
2. Перейдите в раздел **"Drinks"** или **"Напитки"**
3. Нажмите **"+ Добавить напиток"**
4. Для каждого напитка заполните:
   - **Name**: Название (например, "Augustiner Helles")
   - **Category**: Категория (beer / alcoholic / non_alcoholic)
   - **Price**: Цена (например, 4.50)
   - **Alcohol Content**: Содержание алкоголя (например, "5.2%")
   - **Description DE**: Описание на немецком
   - **Description EN**: Описание на английском
   - **Full Description DE**: Полное описание на немецком (опционально)
   - **Full Description EN**: Полное описание на английском (опционально)
   - **Image**: Загрузите изображение напитка
   - **Is Available**: ✅ Доступен
   - **Sort Order**: Порядок сортировки (1, 2, 3...)
5. Нажмите **"Сохранить"**
6. Повторите для всех напитков

**Вариант B: Через SQL (массовая загрузка)**

Если у вас есть список напитков, можете загрузить через SQL:

```sql
INSERT INTO public.drinks (
  name, category, price, alcohol_content, 
  description_de, description_en,
  full_description_de, full_description_en,
  image_url, is_available, sort_order
) VALUES
('Augustiner Helles', 'beer', 4.50, '5.2%',
 'Klassisches Münchner Helles',
 'Classic Munich Helles',
 'Ein traditionelles bayrisches Bier mit mildem Geschmack...',
 'A traditional Bavarian beer with a mild taste...',
 NULL, true, 1),

('Berliner Weisse', 'beer', 4.00, '2.8%',
 'Erfrischendes Berliner Weißbier',
 'Refreshing Berlin wheat beer',
 NULL, NULL, NULL, true, 2),

-- Добавьте остальные напитки...
;
```

### Шаг 6.2: Загрузка изображений напитков

**Важно**: Изображения уже есть в проекте в папке `src/assets/`

**Вариант A: Через админ-панель**
1. При добавлении напитка используйте кнопку загрузки изображения
2. Выберите файл из `src/assets/` (например, `augustiner-beer-transparent.png`)
3. Система автоматически загрузит в Supabase Storage

**Вариант B: Загрузка вручную в Supabase**
1. В Supabase перейдите в **Storage** → **drink-images**
2. Нажмите **"Upload files"**
3. Загрузите изображения из папки `src/assets/`
4. Скопируйте Public URL каждого изображения
5. В SQL обновите:
```sql
UPDATE public.drinks 
SET image_url = 'https://xxxxx.supabase.co/storage/v1/object/public/drink-images/augustiner.png'
WHERE name = 'Augustiner Helles';
```

### Шаг 6.3: Добавление новостей

1. Войдите в админ-панель
2. Перейдите в раздел **"News"** или **"Новости"**
3. Нажмите **"+ Добавить новость"**
4. Заполните:
   - **Title DE/EN**: Заголовок
   - **Excerpt DE/EN**: Краткое описание
   - **Content DE/EN**: Полный текст новости (поддерживает Markdown)
   - **Category**: events / menu / general
   - **Slug**: URL-адрес (например, `oktoberfest-2025`)
   - **Author Name**: Имя автора
   - **Read Time**: Время чтения (минуты)
   - **Image**: Изображение новости
   - **Is Published**: ✅ Опубликовать
5. Сохраните

### Шаг 6.4: Настройка параметров сайта

1. В админ-панели перейдите в **"Settings"** или **"Настройки"**
2. Настройте:
   - **Название сайта** (DE/EN)
   - **Описание сайта** (DE/EN)
   - **Контактная информация**
   - **Адрес паба**
   - **Телефон**
   - **Email**
   - **Часы работы**
   - **Социальные сети**

Или через SQL:

```sql
INSERT INTO public.site_settings (key, value_de, value_en, description) VALUES
('pub_name', 'Berliner Pub', 'Berliner Pub', 'Название заведения'),
('pub_address', 'Hauptstraße 123, 10115 Berlin', 'Hauptstraße 123, 10115 Berlin', 'Адрес'),
('pub_phone', '+49 30 12345678', '+49 30 12345678', 'Телефон'),
('pub_email', 'info@berliner-pub.de', 'info@berliner-pub.de', 'Email'),
('opening_hours', 'Mo-So: 16:00-01:00', 'Mon-Sun: 4PM-1AM', 'Часы работы');
```

---

## Часть 7: Проверка и тестирование

### Шаг 7.1: Проверка основных функций

Откройте сайт и проверьте:

**Главная страница** (`/`)
- ✅ Страница загружается
- ✅ Изображения отображаются
- ✅ Навигация работает
- ✅ Переключение языка DE/EN работает

**Меню** (`/menu`)
- ✅ Отображаются все напитки
- ✅ Категории работают (Beer, Alcoholic, Non-Alcoholic)
- ✅ Изображения напитков загружаются
- ✅ Цены отображаются правильно

**Новости** (`/news`)
- ✅ Список новостей отображается
- ✅ Можно открыть отдельную новость
- ✅ Фильтры работают

**О нас** (`/about`)
- ✅ Информация отображается

**Контакты** (`/contact`)
- ✅ Контактная информация верна
- ✅ Карта отображается (если настроена)

**Админ-панель** (`/admin`)
- ✅ Можно войти
- ✅ Управление напитками работает
- ✅ Управление новостями работает
- ✅ Загрузка изображений работает

### Шаг 7.2: Проверка производительности

1. Откройте Chrome DevTools (F12)
2. Перейдите на вкладку **"Network"**
3. Обновите страницу (Ctrl+F5)
4. Проверьте:
   - Время загрузки < 3 секунд
   - Размер страницы < 2 MB
   - Все ресурсы загружаются (код 200)

### Шаг 7.3: Проверка на мобильных устройствах

1. В Chrome DevTools нажмите кнопку устройства (Ctrl+Shift+M)
2. Выберите разные устройства:
   - iPhone 12 Pro
   - iPad
   - Samsung Galaxy S21
3. Проверьте, что всё отображается корректно

### Шаг 7.4: Проверка SEO

1. Откройте исходный код страницы (Ctrl+U)
2. Проверьте наличие:
   - `<title>` тега
   - `<meta name="description">` тега
   - Open Graph теги для социальных сетей
   - Правильные заголовки H1, H2, H3

---

## Часть 8: Обслуживание и обновления

### Шаг 8.1: Регулярные задачи

**Еженедельно:**
- ✅ Проверяйте, что сайт работает
- ✅ Просматривайте логи на ошибки (если доступны в STRATO)
- ✅ Делайте резервную копию базы данных

**Ежемесячно:**
- ✅ Обновляйте меню, если изменились цены или напитки
- ✅ Публикуйте новости и события
- ✅ Проверяйте SSL сертификат

### Шаг 8.2: Резервное копирование

**База данных Supabase:**
1. Перейдите в Supabase → **Database** → **Backups**
2. Скачайте последнюю резервную копию
3. Храните локально или в облаке

**Файлы сайта:**
1. В FileZilla скачайте всю папку `www/`
2. Сохраните в безопасном месте
3. Дата файла в имени: `berliner-pub-backup-2025-01-15.zip`

### Шаг 8.3: Обновление сайта

Когда нужно обновить код:

1. Внесите изменения в локальной версии проекта
2. Обновите `src/integrations/supabase/client.ts` с production данными
3. Соберите проект:
   ```bash
   npm run build
   ```
4. Загрузите новые файлы из `dist/` на сервер через FileZilla
5. Проверьте сайт (может понадобиться очистить кэш: Ctrl+Shift+R)

### Шаг 8.4: Обновление зависимостей

Раз в 3-6 месяцев:

```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить безопасные версии
npm update

# Собрать и протестировать
npm run build
npm run preview
```

---

## Решение проблем

### Проблема: "Blank page" после загрузки

**Причина**: Неправильный путь к файлам

**Решение**:
1. Проверьте, что `.htaccess` загружен
2. Проверьте права доступа (755 для папок, 644 для файлов)
3. Проверьте, что все файлы из `dist/` загружены в `www/`

### Проблема: "404 Not Found" при переходе по ссылкам

**Причина**: Не работает `.htaccess`

**Решение**:
1. Убедитесь, что файл называется именно `.htaccess` (с точкой!)
2. Проверьте, что mod_rewrite включен в STRATO (обычно включен)
3. Попробуйте очистить кэш браузера

### Проблема: Изображения не загружаются

**Причина**: Проблема с Supabase Storage

**Решение**:
1. Проверьте RLS политики в Supabase Storage
2. Убедитесь, что bucket `drink-images` публичный
3. Проверьте URL изображений в базе данных:
   ```sql
   SELECT id, name, image_url FROM drinks WHERE image_url IS NOT NULL;
   ```

### Проблема: Не удается войти в админ-панель

**Причина**: Неправильные данные или проблема с Supabase

**Решение**:
1. Проверьте, что email и пароль верны
2. Проверьте в Supabase SQL Editor:
   ```sql
   SELECT id, name, email, is_active FROM admin_users;
   ```
3. Сбросьте пароль через SQL:
   ```sql
   UPDATE admin_users 
   SET password_hash = public.simple_hash('новый_пароль')
   WHERE email = 'ваш@email.com';
   ```

### Проблема: Медленная загрузка сайта

**Решение**:
1. Проверьте размер изображений (оптимизируйте > 500 KB)
2. Убедитесь, что кэширование включено в `.htaccess`
3. Рассмотрите использование CDN
4. Проверьте тарифный план STRATO

### Проблема: CORS ошибки в консоли

**Причина**: Supabase не распознает ваш домен

**Решение**:
1. В Supabase перейдите в **Settings** → **API**
2. Добавьте ваш домен в **Site URL**: `https://ваш-домен.de`
3. Добавьте в **Redirect URLs**:
   - `https://ваш-домен.de`
   - `https://ваш-домен.de/admin`

### Проблема: SSL сертификат не работает

**Решение**:
1. В STRATO проверьте статус SSL сертификата
2. Подождите 15-30 минут после активации
3. Проверьте редирект HTTP→HTTPS в `.htaccess`
4. Свяжитесь с поддержкой STRATO, если не помогло

---

## 📞 Поддержка

### Документация:
- **Supabase**: https://supabase.com/docs
- **React**: https://react.dev
- **Vite**: https://vitejs.dev

### Контакты STRATO:
- **Телефон**: 030 300 146 000 (из Германии)
- **Email**: support@strato.de
- **FAQ**: https://www.strato.de/faq/

### В случае проблем:

1. **Проверьте логи**:
   - Browser Console (F12)
   - Supabase → Database → Logs
   - Supabase → Edge Functions → Logs

2. **Проверьте статус**:
   - Supabase Status: https://status.supabase.com
   - STRATO Status: https://www.strato.de/service/status/

3. **Сохраните информацию для поддержки**:
   - Точное описание проблемы
   - Скриншоты ошибок
   - Browser Console logs
   - Какие шаги привели к проблеме

---

## ✅ Контрольный список финального запуска

Перед объявлением сайта "готовым" убедитесь:

### База данных:
- [ ] Все миграции выполнены
- [ ] Создан администратор
- [ ] Загружены все напитки
- [ ] Загружены изображения напитков
- [ ] Добавлены новости (минимум 2-3)
- [ ] Настроены параметры сайта
- [ ] RLS политики активны
- [ ] Storage bucket настроен

### Сервер:
- [ ] Все файлы загружены в `www/`
- [ ] `.htaccess` файл на месте
- [ ] Права доступа установлены (755/644)
- [ ] SSL сертификат активен
- [ ] HTTPS редирект работает

### Функциональность:
- [ ] Все страницы открываются
- [ ] Навигация работает
- [ ] Переключение языка работает
- [ ] Меню отображается корректно
- [ ] Изображения загружаются
- [ ] Админ-панель доступна
- [ ] Можно добавить/редактировать напитки
- [ ] Можно добавить/редактировать новости

### Безопасность:
- [ ] Пароль администратора изменен на сильный
- [ ] Supabase credentials сохранены
- [ ] Сделана резервная копия БД
- [ ] Сделана резервная копия файлов

### Производительность:
- [ ] Загрузка < 3 секунд
- [ ] Мобильная версия работает
- [ ] Нет ошибок в консоли
- [ ] SEO теги настроены

### Дополнительно:
- [ ] Домен настроен правильно
- [ ] robots.txt настроен
- [ ] sitemap.xml создан (опционально)
- [ ] Google Analytics добавлен (опционально)
- [ ] Контактная информация актуальна

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт Berliner Pub должен быть полностью функциональным и доступным по адресу:

**https://ваш-домен.de**

Не забывайте:
- Регулярно обновлять меню
- Публиковать новости и события
- Делать резервные копии
- Следить за безопасностью

**Желаем успехов! Prost! 🍺**

---

*Последнее обновление: Январь 2025*
*Версия инструкции: 1.0*