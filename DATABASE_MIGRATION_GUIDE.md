# Руководство по переносу базы данных на Supabase клиента

## Оглавление
1. [Подготовка нового проекта Supabase](#1-подготовка-нового-проекта-supabase)
2. [Применение миграций базы данных](#2-применение-миграций-базы-данных)
3. [Настройка политик безопасности (RLS)](#3-настройка-политик-безопасности-rls)
4. [Настройка Storage для изображений](#4-настройка-storage-для-изображений)
5. [Создание Edge Functions](#5-создание-edge-functions)
6. [Обновление конфигурации приложения](#6-обновление-конфигурации-приложения)
7. [Миграция данных (опционально)](#7-миграция-данных-опционально)
8. [Проверка и тестирование](#8-проверка-и-тестирование)
9. [Рекомендации по безопасности](#9-рекомендации-по-безопасности)

---

## 1. Подготовка нового проекта Supabase

### 1.1 Создание проекта
1. Войдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Нажмите **"New Project"**
3. Заполните данные:
   - **Project Name**: `berliner-pub-production` (или другое название)
   - **Database Password**: Создайте надежный пароль (минимум 12 символов)
   - **Region**: Выберите ближайший регион (например, `eu-central-1` для Европы)
4. Нажмите **"Create new project"**
5. Дождитесь завершения создания проекта (~2 минуты)

### 1.2 Получение credentials
После создания проекта перейдите в **Settings → API**:

Скопируйте следующие данные:
- **Project URL**: `https://[your-project-ref].supabase.co`
- **Project API keys**:
  - `anon public` key (для публичного доступа)
  - `service_role` key (для административных операций - ДЕРЖИТЕ В СЕКРЕТЕ!)

---

## 2. Применение миграций базы данных

### 2.1 Установка Supabase CLI (если еще не установлен)
```bash
npm install -g supabase
```

### 2.2 Инициализация проекта
```bash
# Перейдите в директорию проекта
cd d:\DEV\berliner-pub-vibes

# Войдите в Supabase CLI
supabase login

# Свяжите проект с удаленным Supabase проектом
supabase link --project-ref [your-project-ref]
```

### 2.3 Применение миграций
Миграции в проекте уже находятся в папке `supabase/migrations/`. Примените их в правильном порядке:

```bash
# Применить все миграции на production базу
supabase db push
```

**Важно**: Миграции будут применены в алфавитном порядке по timestamp в имени файла.

### 2.4 Проверка структуры базы данных
После применения миграций проверьте в Supabase Dashboard:
- **Database → Tables**: Должны быть созданы таблицы `drinks`, `news`, `site_settings`, `admin_users`, `api_keys`, `analytics_events`
- **Database → Enums**: Должны быть созданы типы `drink_category`, `news_category`

---

## 3. Настройка политик безопасности (RLS)

### 3.1 Проверка Row Level Security
Все таблицы должны иметь включенный RLS. Проверьте в **Database → Tables → [table_name] → Policies**.

### 3.2 Основные политики (уже включены в миграции)

#### Таблица `drinks`
```sql
-- Публичный доступ на чтение доступных напитков
CREATE POLICY "Anyone can view available drinks"
ON public.drinks FOR SELECT
USING (is_available = true);

-- Аутентифицированные пользователи могут управлять
CREATE POLICY "Authenticated users can manage drinks"
ON public.drinks FOR ALL
TO authenticated
USING (true) WITH CHECK (true);
```

#### Таблица `news`
```sql
-- Публичный доступ на чтение опубликованных новостей
CREATE POLICY "Anyone can view published news"
ON public.news FOR SELECT
USING (is_published = true);

-- Аутентифицированные пользователи могут управлять
CREATE POLICY "Authenticated users can manage news"
ON public.news FOR ALL
TO authenticated
USING (true) WITH CHECK (true);
```

#### Таблица `admin_users`
```sql
-- Только для аутентифицированных пользователей
CREATE POLICY "Only authenticated users can access admin_users"
ON public.admin_users FOR SELECT
TO authenticated
USING (true);
```

#### Таблица `api_keys`
```sql
-- Только сервис может читать API ключи
CREATE POLICY "Service role can read api keys"
ON public.api_keys FOR SELECT
TO service_role
USING (true);
```

### 3.3 Дополнительные рекомендации по безопасности

1. **Ограничьте доступ к административным таблицам**:
   - Таблица `admin_users` должна быть доступна только через server-side код
   - Таблица `api_keys` НЕ должна быть доступна через публичный API

2. **Включите rate limiting** в Supabase Dashboard:
   - **Settings → API → Rate Limits**
   - Установите разумные лимиты (например, 100 запросов в минуту)

---

## 4. Настройка Storage для изображений

### 4.1 Создание Storage Bucket
1. Перейдите в **Storage** в Supabase Dashboard
2. Нажмите **"New bucket"**
3. Создайте bucket с именем `pub-images`
4. Настройки:
   - ☑ **Public bucket** (для публичного доступа к изображениям)
   - **File size limit**: 5 MB
   - **Allowed MIME types**: `image/jpeg, image/png, image/webp, image/gif`

### 4.2 Настройка политик Storage
```sql
-- Публичный доступ на чтение
CREATE POLICY "Public can view images"
ON storage.objects FOR SELECT
USING (bucket_id = 'pub-images');

-- Аутентифицированные пользователи могут загружать
CREATE POLICY "Authenticated users can upload images"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'pub-images');

-- Аутентифицированные пользователи могут удалять
CREATE POLICY "Authenticated users can delete images"
ON storage.objects FOR DELETE
TO authenticated
USING (bucket_id = 'pub-images');
```

### 4.3 Применение политик
В Supabase Dashboard:
1. **Storage → Policies**
2. Выберите bucket `pub-images`
3. Добавьте политики выше через SQL Editor

---

## 5. Создание Edge Functions

### 5.1 Деплой News API функции
```bash
# Деплой функции news-api
supabase functions deploy news-api

# Установите секреты для API ключа новостей (если используется)
supabase secrets set NEWS_API_KEY=your_news_api_key_here
```

### 5.2 Деплой Generate Beer Image функции
```bash
# Деплой функции generate-beer-image
supabase functions deploy generate-beer-image

# Установите OpenAI API ключ
supabase secrets set OPENAI_API_KEY=your_openai_api_key_here
```

### 5.3 Проверка функций
Проверьте в **Edge Functions** в Dashboard:
- Статус функций должен быть "Active"
- Проверьте логи на наличие ошибок

---

## 6. Обновление конфигурации приложения

### 6.1 Обновление переменных окружения

**Локальный файл `.env`** (для разработки):
```env
VITE_SUPABASE_URL=https://[new-project-ref].supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=[new-anon-key]
VITE_SUPABASE_PROJECT_ID=[new-project-ref]
```

**Для production (на сервере STRATO)**:
1. Обновите файл `.env` на сервере через SSH или FTP
2. Убедитесь, что файл `.env` НЕ доступен публично (добавьте в `.htaccess`):
```apache
<Files ".env">
    Require all denied
</Files>
```

### 6.2 Обновление клиента Supabase в коде

**ВНИМАНИЕ**: Файл `src/integrations/supabase/client.ts` содержит хардкоженные credentials. Необходимо изменить на использование переменных окружения.

Измените файл:
```typescript
// src/integrations/supabase/client.ts
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;

if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
  throw new Error('Missing Supabase environment variables');
}

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    storage: localStorage,
    persistSession: true,
    autoRefreshToken: true,
  }
});
```

### 6.3 Пересборка приложения
```bash
# Установите зависимости (если нужно)
npm install

# Соберите production билд
npm run build

# Результат будет в папке dist/
```

### 6.4 Деплой на STRATO
```bash
# Загрузите содержимое папки dist/ на ваш сервер
# Используйте FTP, SFTP или rsync
```

---

## 7. Миграция данных (опционально)

Если вам нужно перенести данные из тестовой базы в новую:

### 7.1 Экспорт данных из старой БД
```bash
# Используйте Supabase Dashboard старого проекта
# Database → Tables → [table_name] → Export to CSV
```

Или через SQL:
```sql
-- В SQL Editor старого проекта
COPY (SELECT * FROM drinks) TO STDOUT WITH CSV HEADER;
COPY (SELECT * FROM news) TO STDOUT WITH CSV HEADER;
COPY (SELECT * FROM site_settings) TO STDOUT WITH CSV HEADER;
```

### 7.2 Импорт данных в новую БД

**Через Dashboard**:
1. **Database → Tables → [table_name]**
2. **Insert → Import data → From CSV**

**Через SQL**:
```sql
-- В SQL Editor новой базы
INSERT INTO public.drinks (name, category, price, alcohol_content, description_de, description_en, ...)
VALUES
  (...),
  (...);
```

### 7.3 Перенос изображений
Если у вас есть изображения в Storage:
```bash
# Скачайте все изображения из старого Storage
# Загрузите их в новый Storage bucket

# Или используйте Supabase CLI:
supabase storage cp supabase://old-project/pub-images/* supabase://new-project/pub-images/
```

---

## 8. Проверка и тестирование

### 8.1 Чек-лист проверки

- [ ] **База данных**:
  - [ ] Все таблицы созданы
  - [ ] Все индексы применены
  - [ ] RLS включен на всех таблицах
  - [ ] Политики работают корректно

- [ ] **Storage**:
  - [ ] Bucket создан
  - [ ] Политики настроены
  - [ ] Изображения загружаются

- [ ] **Edge Functions**:
  - [ ] Функции задеплоены
  - [ ] Секреты установлены
  - [ ] Функции отвечают на запросы

- [ ] **Приложение**:
  - [ ] `.env` обновлен
  - [ ] Приложение пересобрано
  - [ ] Деплой на сервер выполнен

### 8.2 Тестирование функциональности

1. **Публичный доступ**:
   - Откройте сайт в браузере
   - Проверьте загрузку меню напитков
   - Проверьте загрузку новостей
   - Проверьте отображение изображений

2. **Админ-панель**:
   - Войдите в админ-панель (admin@berlinerpub.de / admin123)
   - Попробуйте добавить новый напиток
   - Попробуйте отредактировать новость
   - Проверьте загрузку изображения

3. **Безопасность**:
   - Проверьте, что анонимные пользователи НЕ могут:
     - Изменять данные
     - Видеть неопубликованные новости
     - Видеть недоступные напитки

---

## 9. Рекомендации по безопасности

### 9.1 Защита учетных данных

1. **НЕ коммитьте секреты в Git**:
```bash
# Убедитесь, что .env в .gitignore
echo ".env" >> .gitignore
echo ".env.local" >> .gitignore
```

2. **Используйте разные ключи для разных окружений**:
   - Development: Тестовый проект
   - Production: Проект клиента

3. **Service Role Key**:
   - НИКОГДА не используйте `service_role` key в клиентском коде
   - Используйте только в server-side функциях или скриптах

### 9.2 Настройка CORS

В Supabase Dashboard:
1. **Settings → API → CORS**
2. Добавьте ваш домен:
```
https://berliner-pub-vibes.com
https://www.berliner-pub-vibes.com
```

### 9.3 Включение дополнительных мер безопасности

1. **JWT Secret Rotation**:
   - **Settings → API → JWT Settings**
   - Регулярно обновляйте JWT secret (каждые 90 дней)

2. **Database Backups**:
   - **Settings → Database → Backups**
   - Включите автоматические бэкапы
   - Настройте расписание (например, ежедневно)

3. **Monitoring**:
   - **Logs & Analytics**
   - Отслеживайте необычную активность
   - Настройте алерты для критических событий

### 9.4 Защита паролей администратора

**ВАЖНО**: После первого входа в систему ОБЯЗАТЕЛЬНО смените пароль по умолчанию!

В админ-панели:
1. Войдите с `admin@berlinerpub.de` / `admin123`
2. Перейдите в настройки профиля
3. Смените пароль на сильный (минимум 12 символов, цифры, спецсимволы)

### 9.5 Настройка HTTPS

Убедитесь, что ваш сайт работает только через HTTPS:
```apache
# В .htaccess уже настроено принудительное перенаправление на HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

### 9.6 SQL Injection Protection

Проект использует Supabase Client, который автоматически защищает от SQL инъекций через параметризованные запросы.

**НЕ делайте**:
```typescript
// ❌ ПЛОХО - уязвимо к SQL injection
supabase.rpc('custom_function', { query: userInput })
```

**Делайте**:
```typescript
// ✅ ХОРОШО - использует параметры
supabase.from('drinks').select('*').eq('name', userInput)
```

### 9.7 Rate Limiting

Настройте в **Settings → API → Rate Limits**:
```
Anonymous requests: 100 per minute
Authenticated requests: 1000 per minute
```

---

## 10. Устранение неполадок

### Проблема: "Invalid API key"
**Решение**: Проверьте, что вы используете правильный `anon` ключ из нового проекта.

### Проблема: "Row Level Security policy violation"
**Решение**: Проверьте RLS политики в таблице. Возможно, нужно добавить дополнительные правила.

### Проблема: Изображения не загружаются
**Решение**:
1. Проверьте, что bucket `pub-images` создан и публичный
2. Проверьте Storage политики
3. Проверьте CORS настройки

### Проблема: Edge Functions не работают
**Решение**:
1. Проверьте логи функций в Dashboard
2. Убедитесь, что все секреты установлены
3. Попробуйте передеплоить функцию

---

## 11. Поддержка и контакты

### Документация Supabase
- [Официальная документация](https://supabase.com/docs)
- [Row Level Security Guide](https://supabase.com/docs/guides/auth/row-level-security)
- [Storage Guide](https://supabase.com/docs/guides/storage)

### Полезные команды CLI
```bash
# Проверка статуса проекта
supabase status

# Просмотр логов Edge Functions
supabase functions logs news-api

# Сброс базы данных (ОСТОРОЖНО!)
supabase db reset

# Создание новой миграции
supabase migration new migration_name
```

---

## Заключение

После выполнения всех шагов ваш сайт будет работать на новой базе данных Supabase клиента с правильно настроенными политиками безопасности.

**Финальный чек-лист**:
- ✅ Новый Supabase проект создан
- ✅ Все миграции применены
- ✅ RLS и политики настроены
- ✅ Storage bucket создан и настроен
- ✅ Edge Functions задеплоены
- ✅ Переменные окружения обновлены
- ✅ Приложение пересобрано и задеплоено
- ✅ Пароль администратора изменен
- ✅ Все функции протестированы

**Не забудьте**:
- 📝 Сохранить все credentials в безопасном месте (password manager)
- 🔒 Удалить старый тестовый проект после успешной миграции
- 📊 Настроить мониторинг и алерты
- 💾 Проверить, что бэкапы работают корректно
