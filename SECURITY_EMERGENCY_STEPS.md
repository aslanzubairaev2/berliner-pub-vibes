# 🚨 ЭКСТРЕННЫЕ МЕРЫ БЕЗОПАСНОСТИ

## ⚠️ КРИТИЧЕСКИ ВАЖНО - ПРОЧИТАЙТЕ СНАЧАЛА!

Ваш файл `.env` с секретными ключами был случайно закоммичен в Git. Это означает, что ваши Supabase API ключи стали публичными и могут быть использованы злоумышленниками.

## ✅ Что уже исправлено автоматически:

1. ✅ `.env` удален из отслеживания Git
2. ✅ `.env` добавлен в `.gitignore`
3. ✅ Создан `.env.example` как шаблон
4. ✅ `.htaccess` настроен для защиты `.env` файлов
5. ✅ `client.ts` использует переменные окружения вместо хардкода

## 🔴 ЧТО ВАМ НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

### Шаг 1: Создать новые API ключи в Supabase

**ПРИЧИНА**: Старые ключи были в Git и возможно скомпрометированы.

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект `hizfxcxcegckhswmsaui`
3. Перейдите в **Settings → API**
4. Прокрутите вниз до раздела **Project API keys**
5. Нажмите на кнопку **Regenerate** рядом с `anon public` ключом
6. **ВАЖНО**: Скопируйте новый ключ - он будет показан только один раз!
7. Сохраните новый ключ в безопасное место

### Шаг 2: Обновить .env файл с новыми ключами

1. Откройте файл `.env` в корне проекта
2. Замените старые значения на новые:

```env
VITE_SUPABASE_URL=https://hizfxcxcegckhswmsaui.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=НОВЫЙ_КЛЮЧ_КОТОРЫЙ_ВЫ_ТОЛЬКО_ЧТО_СОЗДАЛИ
VITE_SUPABASE_PROJECT_ID=hizfxcxcegckhswmsaui
```

3. Сохраните файл

### Шаг 3: Проверить что .env НЕ будет снова закоммичен

Выполните в терминале:

```bash
# Проверьте что .env в списке игнорируемых файлов
git status

# .env НЕ должен появляться в списке измененных файлов
# Если появляется - что-то пошло не так!
```

### Шаг 4: Закоммитить изменения безопасности

```bash
# Добавьте все изменения (кроме .env, который теперь игнорируется)
git add .

# Создайте коммит
git commit -m "Security: Remove hardcoded credentials and protect .env files"

# Отправьте на GitHub
git push origin main
```

### Шаг 5: Пересобрать и задеплоить приложение

```bash
# Пересоберите проект с новыми ключами
npm run build

# Загрузите содержимое папки dist/ на ваш STRATO сервер
# Используйте FTP/SFTP
```

### Шаг 6: Обновить .env на production сервере

**НА СЕРВЕРЕ STRATO**:

1. Подключитесь к серверу через FTP/SFTP
2. Найдите файл `.env` в корне вашего сайта
3. Откройте его для редактирования
4. Замените старые ключи на новые (те же, что в локальном `.env`)
5. Сохраните файл
6. **ВАЖНО**: Убедитесь что `.htaccess` защищает `.env` от публичного доступа

### Шаг 7: Проверить что старые ключи больше не работают

1. Попробуйте использовать старый ключ в Postman или curl:
```bash
curl https://hizfxcxcegckhswmsaui.supabase.co/rest/v1/drinks \
  -H "apikey: СТАРЫЙ_КЛЮЧ" \
  -H "Authorization: Bearer СТАРЫЙ_КЛЮЧ"
```

2. Должны получить ошибку `Invalid API key`
3. Если запрос прошел - ключи еще не обновлены!

---

## 📋 Дополнительные рекомендации

### История Git - очистка (опционально, но рекомендуется)

Если вы хотите удалить `.env` из всей истории Git:

```bash
# ВНИМАНИЕ: Это изменит всю историю Git!
# Сделайте backup перед выполнением!

# Установите git-filter-repo (если еще не установлен)
pip install git-filter-repo

# Удалите .env из всей истории
git filter-repo --path .env --invert-paths

# Форсированно отправьте на GitHub
# ВНИМАНИЕ: Это перезапишет историю на сервере!
git push origin main --force
```

**⚠️ ВАЖНО**: Это изменит всю историю Git и может вызвать проблемы если кто-то еще работает с этим репозиторием.

### Альтернативный вариант (проще, но менее безопасный)

Если вы не хотите переписывать историю Git:

1. Просто замените все ключи в Supabase (как в Шаге 1 выше)
2. Старые ключи в истории Git станут недействительными
3. Новые ключи никогда не будут в Git

Это безопасно, если вы уверены что старые ключи не были скомпрометированы.

---

## 🔒 Проверка безопасности после выполнения шагов

### Чек-лист безопасности:

- [ ] Новые API ключи созданы в Supabase
- [ ] Файл `.env` обновлен с новыми ключами
- [ ] `.env` в `.gitignore` и не появляется в `git status`
- [ ] Изменения закоммичены и отправлены на GitHub
- [ ] Приложение пересобрано с новыми ключами
- [ ] `.env` на production сервере обновлен
- [ ] `.htaccess` защищает `.env` файлы
- [ ] Старые ключи больше не работают (проверено)
- [ ] Новые ключи работают корректно
- [ ] Сайт работает нормально с новыми ключами

### Проверка через браузер:

1. Откройте ваш сайт
2. Откройте DevTools (F12)
3. Перейдите на вкладку Network
4. Проверьте что запросы к Supabase проходят успешно (статус 200)
5. Убедитесь что в заголовках используется новый API ключ

---

## 📞 Помощь и поддержка

Если что-то пошло не так:

1. **Сайт не работает после обновления ключей**:
   - Проверьте что новые ключи правильно скопированы в `.env`
   - Убедитесь что приложение пересобрано (`npm run build`)
   - Проверьте логи браузера на наличие ошибок

2. **Git не игнорирует .env**:
   - Убедитесь что `.env` в `.gitignore`
   - Выполните `git rm --cached .env` еще раз
   - Проверьте `git status` - `.env` не должен появляться

3. **Ключи не работают**:
   - Убедитесь что скопировали `anon public` ключ, а не `service_role`
   - Проверьте что нет лишних пробелов в `.env` файле
   - Перезапустите dev сервер (`npm run dev`)

---

## 💡 На будущее - как избежать этой проблемы

1. **ВСЕГДА** проверяйте `git status` перед коммитом
2. **НИКОГДА** не коммитьте файлы с секретами (`.env`, `credentials.json`, и т.д.)
3. **ВСЕГДА** используйте `.env.example` вместо реального `.env` в репозитории
4. **НАСТРОЙТЕ** pre-commit hooks для автоматической проверки:

```bash
# Установите pre-commit hook
npm install --save-dev husky
npx husky init
echo "grep -r 'VITE_SUPABASE' .env && exit 1 || exit 0" > .husky/pre-commit
```

5. **ИСПОЛЬЗУЙТЕ** секрет-менеджеры (1Password, Bitwarden) для хранения ключей

---

## 🎯 Итоговый статус

После выполнения всех шагов:

✅ Старые ключи недействительны
✅ Новые ключи защищены и работают
✅ `.env` никогда больше не попадет в Git
✅ Сайт работает безопасно

**Время на выполнение**: ~15-20 минут

**Приоритет**: 🔴 КРИТИЧЕСКИЙ - выполните как можно скорее!
