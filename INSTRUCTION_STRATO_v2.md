# 🚀 Инструкция по публикации на STRATO v2 (упрощенная)

**Версия:** 2.0
**Дата:** 19 ноября 2025
**Для проекта:** Berliner Pub Website

---

## 📋 Что изменилось в v2:

✅ Добавлен правильный `.htaccess` с оптимальным кэшированием
✅ Добавлены favicon файлы для всех устройств
✅ Добавлен PWA манифест (`site.webmanifest`)
✅ Обновлены мета-теги в `index.html`
✅ Изображения напитков перенесены в Supabase Storage
✅ База данных остается на текущем Supabase (не меняется)

---

## ⚡ Быстрый старт (5 шагов)

Если вы спешите, выполните только эти шаги:

### 1️⃣ Сборка проекта
```bash
npm run build
```
⏱️ Займет 1-2 минуты

### 2️⃣ Подключение к STRATO через FileZilla
- **Host:** `sftp://ssh.strato.de`
- **Username:** ваш_логин_strato
- **Password:** ваш_пароль_strato
- **Port:** `22`

### 3️⃣ Загрузка файлов
- Левая панель: перейдите в папку проекта → `dist/`
- Правая панель: перейдите в папку `www/` на сервере
- **Выделите ВСЁ** в `dist/` → перетащите в `www/`
- **Загрузите `.htaccess`** из корня проекта в `www/`

### 4️⃣ Проверка прав доступа
- Правый клик на папку `www/` → **File permissions**
- Папки: `755` (rwxr-xr-x)
- Файлы: `644` (rw-r--r--)
- ✅ Отметьте **"Recurse into subdirectories"**

### 5️⃣ Открыть сайт
```
https://ваш-домен.de
```
✅ Готово!

---

## 📂 Структура проекта

### **Что загружается на сервер:**

```
www/                              ← Корневая папка на STRATO
├── .htaccess                     ← КРИТИЧЕСКИ ВАЖНО!
├── index.html                    ← Главная страница
├── assets/                       ← JS, CSS, изображения
│   ├── index-[hash].js          ← Основной JavaScript (с хешем)
│   ├── index-[hash].css         ← Основные стили (с хешем)
│   └── ...другие файлы
├── favicon.ico                   ← Иконка сайта (старый формат)
├── favicon.svg                   ← Иконка сайта (векторная)
├── favicon-96x96.png            ← Иконка 96x96
├── apple-touch-icon.png         ← Иконка для iOS
├── site.webmanifest             ← PWA манифест
├── web-app-manifest-192x192.png ← PWA иконка 192px
├── web-app-manifest-512x512.png ← PWA иконка 512px
└── robots.txt                   ← Для поисковых систем (если есть)
```

---

## 🔧 Детальная инструкция

### **Шаг 1: Подготовка локально**

#### 1.1. Проверка переменных окружения

Убедитесь, что файл `.env` содержит правильные данные Supabase:

```env
VITE_SUPABASE_PROJECT_ID="hizfxcxcegckhswmsaui"
VITE_SUPABASE_PUBLISHABLE_KEY="ваш_ключ_здесь"
VITE_SUPABASE_URL="https://hizfxcxcegckhswmsaui.supabase.co"
```

> **⚠️ Важно:** Файл `.env` **НЕ загружается** на сервер! Значения используются только при сборке.

#### 1.2. Установка зависимостей (если нужно)

```bash
npm install
```

Выполняйте только если:
- Первый раз собираете проект
- Изменили `package.json`

#### 1.3. Сборка production версии

```bash
npm run build
```

**Что происходит:**
- ✅ TypeScript компилируется в JavaScript
- ✅ Код минифицируется и оптимизируется
- ✅ CSS объединяется и минифицируется
- ✅ Создается папка `dist/` с готовым сайтом
- ✅ Файлы получают уникальные хеши (cache busting)

**Результат:**
```
✓ 1234 modules transformed.
dist/index.html                  2.45 kB
dist/assets/index-abc123.js    345.67 kB
dist/assets/index-def456.css    12.34 kB
✓ built in 15.23s
```

#### 1.4. Локальная проверка (опционально)

Перед загрузкой на сервер проверьте локально:

```bash
npm run preview
```

Откройте `http://localhost:4173` и проверьте:
- ✅ Сайт загружается
- ✅ Все страницы работают
- ✅ Изображения отображаются
- ✅ Админ-панель доступна (`/admin`)

---

### **Шаг 2: Подключение к STRATO**

#### 2.1. Получение FTP/SFTP данных

**Где найти данные:**
1. Войдите в [STRATO Customer Center](https://www.strato.de)
2. **Login** → **Kunden-Login**
3. Введите данные доступа
4. **Produkte** → выберите ваш хостинг пакет
5. Найдите раздел **"FTP-Zugänge"** или **"Zugang & Sicherheit"**

**Что нужно:**
- **Hostname:** обычно `ssh.strato.de` (для SFTP)
- **Username:** ваше имя пользователя
- **Password:** ваш FTP/SFTP пароль
- **Port:** `22` (SFTP) или `21` (FTP)

> **💡 Рекомендация:** Используйте **SFTP** (порт 22) вместо FTP для безопасности.

#### 2.2. Установка FileZilla (если не установлен)

1. Скачайте FileZilla: [https://filezilla-project.org/](https://filezilla-project.org/)
2. Установите (бесплатно)
3. Откройте FileZilla

#### 2.3. Подключение через FileZilla

1. **Верхняя панель FileZilla:**
   - **Host:** `sftp://ssh.strato.de`
   - **Username:** ваш_логин
   - **Password:** ваш_пароль
   - **Port:** `22`

2. Нажмите **"Quickconnect"** (Быстрое соединение)

3. **Если появится предупреждение о сертификате:**
   - ✅ Отметьте **"Always trust this host"**
   - Нажмите **"OK"**

4. **После подключения:**
   - **Левая панель:** ваш локальный компьютер
   - **Правая панель:** сервер STRATO

---

### **Шаг 3: Загрузка файлов**

#### 3.1. Навигация в FileZilla

**Левая панель (локальный компьютер):**
```
D:\DEV\berliner-pub-vibes\
├── dist/           ← Перейдите сюда
├── .htaccess       ← Этот файл тоже нужен
```

**Правая панель (сервер STRATO):**
```
/
├── logs/          ← НЕ трогайте
├── ssl/           ← НЕ трогайте
└── www/           ← ПЕРЕЙДИТЕ СЮДА!
```

> **⚠️ ВАЖНО:** Все файлы должны быть загружены в папку `www/`, а не в корень `/`

#### 3.2. Загрузка содержимого dist/

1. **В левой панели:** откройте папку `dist/`
2. **Выделите ВСЁ** в `dist/`:
   - `Ctrl + A` (Windows) или `Cmd + A` (Mac)
3. **Перетащите** на правую панель (в папку `www/`)

#### 3.3. Загрузка .htaccess

1. **В левой панели:** вернитесь на уровень выше (из `dist/`)
2. Найдите файл **`.htaccess`**
3. **Перетащите** `.htaccess` в папку `www/` на сервере

> **⚠️ КРИТИЧЕСКИ ВАЖНО:** Без `.htaccess` React Router НЕ будет работать!

#### 3.4. Ожидание загрузки

Процесс займет **2-5 минут** в зависимости от скорости интернета.

**Прогресс отображается внизу FileZilla:**
```
Передача файлов... 45 из 87 (52%)
Скорость: 1.2 MB/s
Осталось: 1 мин 23 сек
```

#### 3.5. Проверка структуры на сервере

После загрузки структура `www/` должна выглядеть так:

```
www/
├── .htaccess                     ✅
├── index.html                    ✅
├── assets/
│   ├── index-abc123.js          ✅
│   ├── index-def456.css         ✅
│   └── ...
├── favicon.ico                   ✅
├── favicon.svg                   ✅
├── favicon-96x96.png            ✅
├── apple-touch-icon.png         ✅
├── site.webmanifest             ✅
├── web-app-manifest-192x192.png ✅
└── web-app-manifest-512x512.png ✅
```

---

### **Шаг 4: Настройка прав доступа**

Неправильные права доступа могут привести к ошибкам!

#### 4.1. Установка прав через FileZilla

1. **В правой панели:** правый клик на папку `www/`
2. Выберите **"File permissions..."** (Права доступа к файлу)
3. Установите:
   - **Числовое значение:** `755`
   - ✅ Отметьте **"Recurse into subdirectories"**
   - Выберите **"Apply to directories only"**
4. Нажмите **"OK"**

5. **Повторите для файлов:**
   - Правый клик на папку `www/`
   - **"File permissions..."**
   - **Числовое значение:** `644`
   - ✅ Отметьте **"Recurse into subdirectories"**
   - Выберите **"Apply to files only"**
6. Нажмите **"OK"**

#### 4.2. Объяснение прав:

| Права | Числовой код | Описание |
|-------|--------------|----------|
| **Папки** | `755` | `rwxr-xr-x` - Владелец: полный доступ, Остальные: чтение/выполнение |
| **Файлы** | `644` | `rw-r--r--` - Владелец: чтение/запись, Остальные: только чтение |

---

### **Шаг 5: Настройка SSL (HTTPS)**

#### 5.1. Активация SSL сертификата

1. В **STRATO Customer Center** найдите **"SSL-Zertifikate"**
2. Для вашего домена активируйте **Let's Encrypt** (обычно бесплатно)
3. Подождите **5-15 минут** для активации

#### 5.2. Проверка SSL

Откройте в браузере:
```
https://ваш-домен.de
```

✅ **Должен появиться значок замка** в адресной строке

#### 5.3. Редирект HTTP → HTTPS

Файл `.htaccess` уже содержит правило редиректа:

```apache
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

**Что это делает:**
- Все запросы по `http://` автоматически перенаправляются на `https://`
- Код `301` означает постоянный редирект (хорошо для SEO)

---

### **Шаг 6: Проверка сайта**

#### 6.1. Открытие сайта

```
https://ваш-домен.de
```

#### 6.2. Чек-лист проверки:

**Главная страница (`/`):**
- ✅ Страница загружается
- ✅ Дизайн отображается корректно
- ✅ Изображения видны
- ✅ Навигация работает
- ✅ Переключение языка DE/EN работает

**Меню (`/menu`):**
- ✅ Список напитков отображается
- ✅ Категории работают (Beer, Alcoholic, Non-Alcoholic)
- ✅ Изображения из Supabase загружаются
- ✅ Цены отображаются

**Новости (`/news`):**
- ✅ Список новостей показывается
- ✅ Можно открыть отдельную новость
- ✅ Фильтры работают

**Контакты (`/contact`):**
- ✅ Контактная информация видна
- ✅ Карта отображается (если настроена)

**Админ-панель (`/admin`):**
- ✅ Страница входа загружается
- ✅ Можно войти с учетными данными
- ✅ Управление напитками работает
- ✅ Управление новостями работает

**Favicon:**
- ✅ Иконка отображается во вкладке браузера
- ✅ На iOS устройствах показывается `apple-touch-icon`

#### 6.3. Проверка через Chrome DevTools

1. Откройте сайт
2. Нажмите **F12** (открыть DevTools)
3. Вкладка **"Console":**
   - ❌ **НЕ должно быть красных ошибок**
   - ✅ Могут быть предупреждения (warnings) - это нормально

4. Вкладка **"Network":**
   - Нажмите **Ctrl+R** (перезагрузка)
   - Проверьте:
     - `index.html` - **200** (from server)
     - `index-[hash].js` - **200** (from server или from cache)
     - `index-[hash].css` - **200** (from server или from cache)
   - ❌ Не должно быть статусов **404** или **500**

5. Вкладка **"Application"** → **"Manifest":**
   - ✅ Должен показываться `site.webmanifest`
   - ✅ Иконки PWA видны

#### 6.4. Проверка на мобильных устройствах

**Способ 1: Chrome Device Mode**
1. В Chrome DevTools нажмите **Toggle device toolbar** (Ctrl+Shift+M)
2. Выберите устройства:
   - iPhone 12 Pro
   - iPad
   - Samsung Galaxy S21
3. Проверьте отображение на всех экранах

**Способ 2: Реальное устройство**
1. Откройте сайт на телефоне
2. Проверьте:
   - ✅ Адаптивный дизайн работает
   - ✅ Кнопки кликабельны
   - ✅ Меню открывается

---

## 🔄 Обновление сайта (после изменений в коде)

### **Процесс обновления (3 минуты):**

```bash
# 1. Сборка
npm run build

# 2. Загрузка через FileZilla
# - Подключитесь к STRATO
# - Выделите ВСЁ в dist/
# - Перетащите в www/
# - FileZilla перезапишет только измененные файлы

# 3. Очистите кэш браузера (Ctrl+Shift+R)
# 4. Проверьте изменения
```

### **Что загружать при разных типах изменений:**

| Изменение | Что загружать | Время |
|-----------|---------------|-------|
| **Изменили код, дизайн** | Весь `dist/` | 2-3 мин |
| **Добавили изображение в код** | Весь `dist/` | 2-3 мин |
| **Изменили .htaccess** | Только `.htaccess` | 10 сек |
| **Изменили базу данных** | Ничего (выполните SQL в Supabase) | 0 мин |
| **Добавили данные через админку** | Ничего (данные уже в Supabase) | 0 мин |

### **Что НЕ нужно делать повторно:**

- ❌ Создавать `.htaccess` заново
- ❌ Настраивать права доступа
- ❌ Настраивать SSL
- ❌ Переустанавливать зависимости (кроме изменений в `package.json`)

---

## 🔥 Решение проблем с кэшированием

### **Проблема: "Клиенты не видят обновления"**

**Причина:** Браузер показывает старую версию из кэша

**Решение (уже реализовано в .htaccess):**

```apache
# HTML - НЕ кэшируется
<FilesMatch "\.(html|htm)$">
  Header set Cache-Control "no-cache, no-store, must-revalidate"
</FilesMatch>

# JS и CSS - кэшируются на 1 год (они имеют хеши в именах)
<FilesMatch "\.(js|css)$">
  Header set Cache-Control "public, max-age=31536000, immutable"
</FilesMatch>
```

**Как это работает:**

1. **Клиент открывает сайт:**
   - Браузер загружает `index.html` (НЕ из кэша!)
   - `index.html` содержит ссылки: `index-abc123.js`, `index-def456.css`
   - Браузер загружает эти файлы и кэширует на 1 год

2. **Вы обновили сайт:**
   - Vite создал новые файлы: `index-NEW789.js`, `index-NEW012.css`
   - Новый `index.html` ссылается на новые файлы

3. **Клиент обновляет страницу (F5):**
   - Браузер загружает новый `index.html` (НЕ из кэша!)
   - Видит новые имена файлов: `index-NEW789.js`
   - Загружает новые файлы автоматически!

✅ **Клиенты автоматически получают обновления! Кэш очищать НЕ нужно!**

---

## 📋 Контрольный список финальной публикации

Перед объявлением сайта "готовым" проверьте:

### **Файлы на сервере:**
- [ ] Все файлы из `dist/` загружены в `www/`
- [ ] `.htaccess` находится в `www/`
- [ ] Права доступа установлены (755 для папок, 644 для файлов)

### **SSL и безопасность:**
- [ ] SSL сертификат активен (сайт открывается по `https://`)
- [ ] Редирект HTTP→HTTPS работает
- [ ] В консоли браузера нет ошибок

### **Функциональность:**
- [ ] Главная страница загружается
- [ ] Все страницы доступны (/, /menu, /news, /contact, /about)
- [ ] Навигация работает
- [ ] Переключение языка DE/EN работает
- [ ] Изображения отображаются
- [ ] Favicon виден во вкладке браузера

### **Админ-панель:**
- [ ] Доступна по `/admin`
- [ ] Можно войти с учетными данными
- [ ] Управление напитками работает
- [ ] Управление новостями работает
- [ ] Загрузка изображений работает

### **База данных Supabase:**
- [ ] Напитки отображаются на странице `/menu`
- [ ] Новости отображаются на странице `/news`
- [ ] Изображения загружаются из Supabase Storage

### **Производительность:**
- [ ] Время загрузки главной страницы < 3 секунд
- [ ] Мобильная версия работает корректно
- [ ] Нет красных ошибок в консоли браузера

### **SEO:**
- [ ] `<title>` тег настроен
- [ ] `<meta name="description">` настроен
- [ ] Open Graph теги настроены
- [ ] Favicon установлен

---

## 🆘 Решение проблем

### **Проблема 1: "Blank page" (пустая страница)**

**Симптомы:** После открытия сайта видна пустая белая страница

**Возможные причины:**

1. **Неправильный путь к файлам**
   ```bash
   # Проверьте структуру на сервере
   www/
   ├── .htaccess    ← Должен быть здесь!
   ├── index.html   ← Должен быть здесь!
   └── assets/      ← Должна быть здесь!
   ```

2. **Файлы загружены не в `www/`**
   - ❌ Неправильно: `/index.html`
   - ✅ Правильно: `/www/index.html`

3. **Неправильные права доступа**
   - Папки: `755`
   - Файлы: `644`

**Решение:**
1. Проверьте структуру в FileZilla
2. Убедитесь, что все файлы в `www/`
3. Установите правильные права доступа
4. Очистите кэш браузера (Ctrl+Shift+R)

---

### **Проблема 2: "404 Not Found" при переходе по ссылкам**

**Симптомы:**
- Главная страница работает
- При переходе на `/menu` или `/news` появляется ошибка 404

**Причина:** `.htaccess` не работает или не загружен

**Решение:**

1. **Проверьте наличие `.htaccess` на сервере:**
   ```
   www/
   └── .htaccess   ← Должен быть здесь!
   ```

2. **Убедитесь, что файл называется именно `.htaccess`:**
   - ✅ Правильно: `.htaccess` (с точкой в начале!)
   - ❌ Неправильно: `htaccess` (без точки)
   - ❌ Неправильно: `.htaccess.txt`

3. **Проверьте содержимое `.htaccess`:**
   - Должен содержать блок `RewriteEngine On`
   - Должен содержать правила `RewriteRule`

4. **В FileZilla включите показ скрытых файлов:**
   - **Server** → **Force showing hidden files**

---

### **Проблема 3: Изображения не загружаются**

**Симптомы:** На странице `/menu` напитки без изображений

**Причина:** Проблема с Supabase Storage или неправильные URL

**Решение:**

1. **Проверьте Supabase Storage:**
   - Откройте Supabase Dashboard
   - **Storage** → **drink-images**
   - Убедитесь, что bucket существует и публичный

2. **Проверьте RLS политики:**
   ```sql
   -- В Supabase SQL Editor
   SELECT * FROM storage.objects WHERE bucket_id = 'drink-images';
   ```

3. **Проверьте URL изображений в базе:**
   ```sql
   SELECT id, name, image_url FROM drinks WHERE image_url IS NOT NULL;
   ```

4. **Попробуйте открыть URL изображения напрямую:**
   ```
   https://hizfxcxcegckhswmsaui.supabase.co/storage/v1/object/public/drink-images/augustiner.png
   ```
   - Если открывается → проблема в коде
   - Если 404 → проблема в Storage

---

### **Проблема 4: Не удается войти в админ-панель**

**Симптомы:** Ошибка при попытке входа в `/admin`

**Причина:** Неправильные учетные данные или проблема с Supabase

**Решение:**

1. **Проверьте учетные данные в Supabase:**
   ```sql
   SELECT id, name, email, is_active FROM admin_users;
   ```

2. **Сбросьте пароль администратора:**
   ```sql
   UPDATE admin_users
   SET password_hash = public.simple_hash('новый_пароль')
   WHERE email = 'admin@ваш-домен.de';
   ```

3. **Проверьте подключение к Supabase:**
   - Откройте Chrome DevTools → **Network**
   - Обновите страницу `/admin/login`
   - Ищите запросы к `supabase.co`
   - Если ошибки 401/403 → проблема с ключами
   - Если ошибки 500 → проблема с базой данных

4. **Проверьте Supabase URL и Key в коде:**
   ```typescript
   // src/integrations/supabase/client.ts
   const SUPABASE_URL = "https://hizfxcxcegckhswmsaui.supabase.co";
   const SUPABASE_PUBLISHABLE_KEY = "ваш_ключ";
   ```

---

### **Проблема 5: SSL сертификат не работает**

**Симптомы:**
- Сайт показывает предупреждение о безопасности
- Нет значка замка в адресной строке

**Решение:**

1. **Проверьте статус SSL в STRATO:**
   - Customer Center → **SSL-Zertifikate**
   - Убедитесь, что сертификат активен

2. **Подождите 15-30 минут:**
   - SSL сертификаты активируются не мгновенно

3. **Очистите кэш браузера:**
   ```
   Ctrl + Shift + Delete
   ```

4. **Проверьте редирект в `.htaccess`:**
   ```apache
   RewriteEngine On
   RewriteCond %{HTTPS} off
   RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
   ```

5. **Свяжитесь с поддержкой STRATO:**
   - Телефон: 030 300 146 000
   - Email: support@strato.de

---

### **Проблема 6: Медленная загрузка сайта**

**Симптомы:** Сайт загружается дольше 5 секунд

**Причины и решения:**

1. **Большие изображения:**
   - Оптимизируйте изображения (< 500 KB)
   - Используйте формат WebP вместо PNG/JPG

2. **Кэширование не работает:**
   - Проверьте `.htaccess`
   - Проверьте заголовки в Chrome DevTools → Network

3. **Слабый тарифный план STRATO:**
   - Рассмотрите апгрейд тарифа

4. **Много данных в базе:**
   - Оптимизируйте SQL запросы
   - Добавьте индексы в Supabase

---

### **Проблема 7: CORS ошибки в консоли**

**Симптомы:**
```
Access to fetch at 'https://...supabase.co' from origin 'https://ваш-домен.de'
has been blocked by CORS policy
```

**Причина:** Supabase не распознает ваш домен

**Решение:**

1. **Добавьте домен в Supabase:**
   - Supabase Dashboard → **Settings** → **API**
   - **Site URL:** `https://ваш-домен.de`
   - **Redirect URLs:** добавьте:
     - `https://ваш-домен.de`
     - `https://ваш-домен.de/admin`
     - `https://ваш-домен.de/**`

2. **Сохраните изменения и подождите 2-3 минуты**

3. **Очистите кэш браузера (Ctrl+Shift+R)**

---

## 📞 Поддержка

### **Документация:**
- **Vite:** [https://vitejs.dev](https://vitejs.dev)
- **React:** [https://react.dev](https://react.dev)
- **Supabase:** [https://supabase.com/docs](https://supabase.com/docs)

### **STRATO поддержка:**
- **Телефон:** 030 300 146 000 (из Германии)
- **Email:** support@strato.de
- **FAQ:** [https://www.strato.de/faq/](https://www.strato.de/faq/)

### **В случае проблем сохраните:**
- Скриншоты ошибок
- Логи из Chrome DevTools (Console)
- Описание шагов, приведших к проблеме

---

## 📊 Сравнение v1 vs v2

| Компонент | v1 | v2 |
|-----------|----|----|
| **Кэширование** | ❌ Базовое | ✅ Оптимальное с cache busting |
| **Favicon** | ❌ Только .ico | ✅ Полный набор для всех устройств |
| **PWA** | ❌ Нет | ✅ Манифест и иконки |
| **SEO мета-теги** | ⚠️ Русский язык | ✅ Английский язык |
| **Изображения** | ⚠️ В src/assets | ✅ В Supabase Storage |
| **База данных** | ✅ Supabase | ✅ Та же Supabase (не меняется) |

---

## ✅ Готово!

После выполнения всех шагов ваш сайт **Berliner Pub** полностью функционален и доступен по адресу:

```
https://ваш-домен.de
```

**Основные преимущества v2:**
- 🚀 Автоматическое кэширование (клиенты видят обновления сразу)
- 📱 Поддержка PWA (можно добавить на главный экран телефона)
- ⚡ Быстрая загрузка (благодаря оптимизации кэша)
- 🔒 Безопасность (SSL + правильные заголовки)
- 🎨 Правильные favicon для всех устройств

**Не забывайте:**
- Регулярно обновлять меню и цены
- Публиковать новости и события
- Делать резервные копии
- Следить за безопасностью

**Prost! 🍺**

---

*Версия инструкции: 2.0*
*Дата: 19 ноября 2025*
*Проект: Berliner Pub Website*
